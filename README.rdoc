To see the error I am having with refinerycms, please follow these steps:

    git clone git@github.com:jfedick/example.git
    cd example/
    bundle install
    bundle exec rake db:migrate
    rails s

Then, go to http://localhost:3000 and you should see it.
